**v1.0.1**

- POS boot sequence text is cleared on program entry
- adjusted formatting so high performance engine VINs don't push text around
- fixed centered text to be actually centered on screen
- fixed player being able to type VIN when not using the computer
- added variation to internet speed (makes it slower)
- added small delays between request states (makes it slower-er)
- added animation to VIN table screen (makes it even slower-er)
